/**
 * A component is something that holds state that can be attached to an entity.
 * 
 * @remarks
 * This is a Marker Interface, it isn't designed to have any methods. It
 * will provide type information about objects that implement it; specifically it
 * just identifies them as being components.
 * 
 * See below for more information about this pattern:
 * https://en.wikipedia.org/wiki/Marker_interface_pattern
 * 
 */
interface Component { }