/**
 * Updates required entities based upon user input.
 *
 */
class UserInputSystem implements System {
  /** {@inheritDoc} */
  update(entities: Set<Entity>, ecsContainer: EcsContainer): void {

    // TODO: implement rendering of the entities. This should be done via
    // the following steps:
    //
    // 1. Read keys and mouse input.
    // 2. Apply appropriate movement to the Boat entity.
  }
}
